{
  "version": 2,
  "build": {
    "env": {
      "BUILDING_FOR_NOW": "true"
    }
  },
  "builds": [
    { "src": "app/package.json", "use": "@now/next" },
    { "src": "web/package.json", "use": "@now/next" }
  ],
  "routes": [
    { "src": "/timelines/_next(.*)", "dest": "timelines/_next$1" },
    { "src": "/timelines(.*)", "dest": "timelines$1" },
    { "src": "(.*)", "dest": "web$1" }
  ]
}
